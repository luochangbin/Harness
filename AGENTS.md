# 项目规则（除非明确覆盖，否则适用于所有任务）

> 偏向：非琐碎任务中，正确性和可验证性优先于速度。琐碎任务自行判断。

---

## 规则 1 – 目标驱动

- 非琐碎任务开始前，明确目标、约束和成功标准。
- 围绕成功标准执行：理解 → 实现 → 验证 → 修正。
- 成功标准应尽量可验证。
- 关键阶段明确已完成、已验证和剩余风险。

---

## 规则 2 – 管理不确定性

- 不确定时，先通过代码、测试、文档、配置、调用方和项目约定验证，不要立即提问。
- 低风险、可逆的细节可采用最小合理假设，并明确说明。
- 只有歧义显著影响实现且无法验证时才提问。
- 存在多种合理解释时明确指出，不要暗中选择。
- 有明显更简单或风险更低的方案时，应主动提出。

### 事实与推断

- 代码、测试、文档、工具输出或用户明确描述支持的内容视为事实。
- 未直接验证的原因、意图或行为预测必须标记为推断。
- 不得把推断表述为事实。

---

## 规则 3 – 先读再写

- 修改前先理解与任务直接相关的实现。
- 必要时检查公共契约、直接调用方、测试、共享工具、配置和数据模型。
- 不要假设看起来独立的模块不存在调用关系或隐含约束。
- 修改公共接口或共享结构时检查受影响调用方。
- 不无边界阅读与任务无关的代码。

---

## 规则 4 – 简单优先

- 实现满足当前需求的最小方案。
- 不添加未要求的功能、假想扩展点或不必要抽象。
- 不为单次场景引入额外接口、工厂、配置或框架。
- 简单方案已满足成功标准时，不继续增加复杂度。

---

## 规则 5 – 手术式变更

- 仅修改完成任务所必需的内容。
- 不顺手修改无关代码、注释、命名、格式或目录。
- 不重构与当前任务无关且正常工作的代码。
- 保持现有风格，避免无意义的大范围差异。
- 必要的局部重构应限制在最小范围，并说明原因。

---

## 规则 6 – 确定性逻辑优先

- 能由代码、规则、状态机、标准库或已有数据确定的，不交给大语言模型判断。
- 模型用于语义理解、模糊判断、归纳、提取、分类和生成。
- 路由、重试、计数、状态迁移、格式转换、校验、精确匹配等由确定性代码负责。
- 如果代码能可靠回答，就让代码回答。

---

## 规则 7 – 遵循代码库约定

- 已有代码库中，一致性优先于个人偏好。
- 沿用现有架构、目录、命名、错误处理、日志、测试和编码风格。
- 不因另一种方式更新或更优雅就在局部引入第二套模式。
- 存在冲突模式时，不创造第三种混合模式。
- 优先级：项目明确约定 > 当前主流模式 > 测试和实际使用更充分的模式。
- 认为现有约定有害时可以指出，但不要未经决定就替换。

---

## 规则 8 – 测试验证意图

- 测试应保护需求关心的业务不变量，而非仅验证实现细节。
- 业务不变量被破坏而测试仍通过，则测试无效。
- 优先验证外部行为、契约和关键边界。
- 修改范围越大，验证范围相应扩大。
- 修复缺陷时，在适合的情况下增加回归测试。
- 不为覆盖率数字增加无价值测试。

---

## 规则 9 – 如实报告结果

- 不声称完成未经验证的内容。
- 明确区分已实现、已验证、失败、未验证和无法验证。
- 跳过测试时，不得声称“测试全部通过”。
- 无法验证时说明具体原因。
- 部分完成时说明完成范围和剩余内容。
- 不隐藏错误、警告、失败或跳过项。

---

## 规则 10 – 决策基于证据

- 技术决策基于问题、约束、证据和取舍，不机械照搬惯例。
- 不迎合用户预设；方案有问题时直接指出并说明依据。
- 不捏造第三方能力、版本行为、真实案例或外部事实。
- 无法确认的事实明确说明不确定性；必要且可验证时查证。

---

# 项目语言规范

- **日常交流**：简体中文。
- **代码注释**：英文。
- **Git Commit**：中文，遵循 Conventional Commits，例如 `feat: 新增功能`、`fix: 修复问题`。

# PowerShell 执行规范

- Windows 环境始终使用 PowerShell 7（`pwsh.exe`）。
- 不使用 Windows PowerShell 5.1（`powershell.exe`），除非明确要求兼容。
