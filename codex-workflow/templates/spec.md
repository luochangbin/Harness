# AR-XXX：<变更标题>

## 变更意图

### 问题
<当前问题或机会，1-3 句>

### 目标
- <可由 Requirement/Scenario 验证的目标>

### 非目标
- <本次明确不做的事项>

### 范围
- 影响模块：<模块 id 列表>
- 边界：<用户、平台、数据或兼容边界>

## 增量规格

> 单模块：节头可不标注，默认归属唯一影响模块。
> 多模块：每个 ADDED/MODIFIED 节头必须标注模块，例如
> `## ADDED Requirements（模块：auth）`。

## ADDED Requirements

### Requirement: <需求名>

<系统应该做什么>

#### Scenario: <场景名>

- **WHEN** <触发条件>
- **THEN** <预期结果>
- **AND** <附加结果（可选）>

## MODIFIED Requirements

### Requirement: <需求名>

<修改后的完整描述，含变更点说明>